---
phase: 02-catalog-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/
  - src/lib/utils.ts
  - src/components/star-rating.tsx
  - src/components/status-badge.tsx
  - src/components/book-cover.tsx
  - next.config.ts
  - components.json
autonomous: true
requirements:
  - CATALOG-03
  - CATALOG-04

must_haves:
  truths:
    - "shadcn/ui is initialized with dark zinc theme and Geist Sans font"
    - "All required shadcn components are installed: dialog, select, alert-dialog, input, button, label, textarea, card, badge"
    - "StarRating component renders 5 clickable stars and reports selected value"
    - "StatusBadge component renders colored badge for each of the 5 status values"
    - "BookCover component renders next/image with fallback placeholder"
    - "next/image remotePatterns configured for books.google.com and covers.openlibrary.org"
  artifacts:
    - path: "src/components/star-rating.tsx"
      provides: "Clickable 5-star rating component"
      exports: ["StarRating"]
    - path: "src/components/status-badge.tsx"
      provides: "Colored badge per book status"
      exports: ["StatusBadge"]
    - path: "src/components/book-cover.tsx"
      provides: "Book cover image with placeholder fallback"
      exports: ["BookCover"]
    - path: "next.config.ts"
      provides: "Image remote patterns for Google Books and Open Library"
      contains: "remotePatterns"
    - path: "components.json"
      provides: "shadcn/ui configuration"
    - path: "src/lib/utils.ts"
      provides: "cn() utility from shadcn init"
      exports: ["cn"]
  key_links:
    - from: "src/components/star-rating.tsx"
      to: "lucide-react"
      via: "import { Star } from 'lucide-react'"
      pattern: "import.*Star.*lucide"
    - from: "src/components/status-badge.tsx"
      to: "src/lib/books/schema.ts"
      via: "import type { BookStatus }"
      pattern: "import.*BookStatus"
---

<objective>
Initialize shadcn/ui, configure next/image remote patterns, and create shared UI components (StarRating, StatusBadge, BookCover) used across multiple pages.

Purpose: These components are the UI building blocks for both the home page book list and the book detail page. Setting up shadcn/ui first lets subsequent plans import Dialog, Select, AlertDialog etc. directly.

Output: shadcn/ui fully initialized, 9 shadcn components installed, 3 custom shared components created, next.config.ts updated with image remote patterns.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-catalog-core/02-RESEARCH.md
@.planning/phases/02-catalog-core/02-PATTERNS.md
@.planning/phases/02-catalog-core/02-UI-SPEC.md

<interfaces>
<!-- Types needed by shared components -->

From src/lib/books/schema.ts:
```typescript
export type BookStatus = 'quero-ler' | 'lendo' | 'lido' | 'quero-reler' | 'abandonado'
```

From src/app/layout.tsx (line 30):
```typescript
<body className="min-h-full flex flex-col bg-zinc-950 text-zinc-100">
```

Zinc dark theme tokens (from UI-SPEC.md):
- bg-zinc-950: page background
- bg-zinc-900: cards, dialogs
- bg-zinc-800: inputs, secondary areas
- border-zinc-800: card borders
- border-zinc-700: input borders
- text-zinc-100: primary text
- text-zinc-400: secondary text
- yellow-400: star fill color
- zinc-600: empty star color
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and configure next.config.ts</name>
  <files>components.json, src/lib/utils.ts, src/components/ui/, next.config.ts</files>
  <read_first>
    - next.config.ts (current state -- will add images.remotePatterns)
    - src/app/layout.tsx (to confirm dark theme class and font setup)
    - src/app/globals.css (to see current Tailwind setup)
    - package.json (to confirm what's already installed)
  </read_first>
  <action>
**Step 1: Initialize shadcn/ui**

Run the shadcn init command. When prompted:
- Style: Default
- Base color: Zinc
- CSS variables: Yes (for dark mode support)

```bash
cd "/Users/mauro/projects/Dona Flora" && npx shadcn@latest init --defaults
```

If the command asks for interactive input, use the `--defaults` flag or answer: style=default, base-color=zinc, css-variables=yes.

After init, verify that `components.json` was created and `src/lib/utils.ts` exists with the `cn()` function.

**Step 2: Install required shadcn components**

```bash
cd "/Users/mauro/projects/Dona Flora" && npx shadcn@latest add dialog select alert-dialog input button label textarea card badge
```

This installs all 9 components into `src/components/ui/`. It also auto-installs `radix-ui` and `lucide-react` as dependencies.

**Step 3: Update next.config.ts**

Read the current `next.config.ts` (currently has only `output: 'standalone'`) and update it to:

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'standalone',
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'books.google.com' },
      { protocol: 'https', hostname: 'covers.openlibrary.org' },
    ],
  },
}

export default nextConfig
```

This enables `next/image` to load book covers from Google Books and Open Library (per RESEARCH.md Pitfall 6).

**Step 4: Verify installation**

Run `npm run build` to confirm no compilation errors from the shadcn setup. If build fails due to unrelated issues, at minimum run `npx tsc --noEmit` to check TypeScript compiles.
  </action>
  <verify>
    <automated>cd "/Users/mauro/projects/Dona Flora" && test -f components.json && test -f src/lib/utils.ts && test -d src/components/ui && ls src/components/ui/ | wc -l | xargs test 9 -le && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists in project root
    - src/lib/utils.ts exists and contains `export function cn(`
    - src/components/ui/ directory contains at least 9 files (dialog, select, alert-dialog, input, button, label, textarea, card, badge)
    - next.config.ts contains `remotePatterns` array
    - next.config.ts contains `hostname: 'books.google.com'`
    - next.config.ts contains `hostname: 'covers.openlibrary.org'`
    - package.json contains radix-ui in dependencies
    - package.json contains lucide-react in dependencies
  </acceptance_criteria>
  <done>shadcn/ui initialized with zinc dark theme. All 9 required components installed. next.config.ts has image remote patterns for book cover domains.</done>
</task>

<task type="auto">
  <name>Task 2: Create StarRating, StatusBadge, and BookCover shared components</name>
  <files>src/components/star-rating.tsx, src/components/status-badge.tsx, src/components/book-cover.tsx</files>
  <read_first>
    - src/lib/books/schema.ts (BookStatus type -- 5 enum values)
    - src/app/page.tsx (existing zinc theme classes for visual consistency)
    - src/app/layout.tsx (confirm dark theme body classes)
    - src/components/ui/badge.tsx (shadcn badge component to understand its API for StatusBadge)
    - src/lib/utils.ts (cn() utility)
  </read_first>
  <action>
**Create `src/components/star-rating.tsx`** (per D-06 -- clickable stars):

```typescript
'use client'

import { Star } from 'lucide-react'
import { cn } from '@/lib/utils'

interface StarRatingProps {
  value: number
  onChange?: (value: number) => void
  readonly?: boolean
}

export function StarRating({ value, onChange, readonly }: StarRatingProps) {
  return (
    <div className="flex gap-1" role="group" aria-label="Avaliacao">
      {[1, 2, 3, 4, 5].map((star) => (
        <button
          key={star}
          type="button"
          disabled={readonly}
          onClick={() => onChange?.(star)}
          aria-label={`Nota ${star} de 5`}
          className={cn(
            'min-w-[44px] min-h-[44px] flex items-center justify-center transition-colors',
            readonly ? 'cursor-default' : 'cursor-pointer hover:text-yellow-400'
          )}
        >
          <Star
            className={cn(
              'h-5 w-5',
              star <= value
                ? 'fill-yellow-400 text-yellow-400'
                : 'text-zinc-600'
            )}
          />
        </button>
      ))}
    </div>
  )
}
```

Key details per UI-SPEC.md:
- Each star button has `min-w-[44px] min-h-[44px]` for touch target accessibility (44x44px minimum)
- `role="group"` + `aria-label="Avaliacao"` for screen reader fieldset equivalent
- Each button has `aria-label="Nota {n} de 5"` per accessibility contract
- Filled stars: `fill-yellow-400 text-yellow-400` / Empty: `text-zinc-600`
- Icon size: `h-5 w-5` (20x20px)
- `type="button"` to prevent form submission

**Create `src/components/status-badge.tsx`:**

```typescript
import { cn } from '@/lib/utils'
import { Badge } from '@/components/ui/badge'
import type { BookStatus } from '@/lib/books/schema'

const STATUS_CONFIG: Record<BookStatus, { label: string; className: string }> = {
  'quero-ler': { label: 'Quero ler', className: 'bg-zinc-800 text-zinc-300' },
  'lendo': { label: 'Lendo', className: 'bg-blue-900/50 text-blue-400' },
  'lido': { label: 'Lido', className: 'bg-green-900/50 text-green-400' },
  'quero-reler': { label: 'Quero reler', className: 'bg-amber-900/50 text-amber-400' },
  'abandonado': { label: 'Abandonado', className: 'bg-zinc-800 text-zinc-500' },
}

interface StatusBadgeProps {
  status: BookStatus
  className?: string
}

export function StatusBadge({ status, className }: StatusBadgeProps) {
  const config = STATUS_CONFIG[status]
  return (
    <Badge variant="secondary" className={cn(config.className, className)}>
      {config.label}
    </Badge>
  )
}
```

Key details per UI-SPEC.md status badge colors:
- quero-ler: bg-zinc-800 text-zinc-300
- lendo: bg-blue-900/50 text-blue-400
- lido: bg-green-900/50 text-green-400
- quero-reler: bg-amber-900/50 text-amber-400
- abandonado: bg-zinc-800 text-zinc-500
- Labels in pt-BR per copywriting contract: "Quero ler", "Lendo", "Lido", "Quero reler", "Abandonado"

**Create `src/components/book-cover.tsx`:**

```typescript
import Image from 'next/image'
import { BookOpen } from 'lucide-react'
import { cn } from '@/lib/utils'

interface BookCoverProps {
  src?: string
  alt: string
  size?: 'sm' | 'md' | 'lg'
  className?: string
}

const SIZES = {
  sm: { width: 48, height: 72 },    // home page list item
  md: { width: 128, height: 192 },  // mobile detail
  lg: { width: 192, height: 288 },  // desktop detail
}

export function BookCover({ src, alt, size = 'md', className }: BookCoverProps) {
  const { width, height } = SIZES[size]

  if (!src) {
    return (
      <div
        className={cn(
          'flex items-center justify-center rounded-lg bg-zinc-800',
          className
        )}
        style={{ width, height }}
        aria-label="Sem capa disponivel"
      >
        <BookOpen className="h-8 w-8 text-zinc-600" />
      </div>
    )
  }

  return (
    <Image
      src={src}
      alt={`${alt} - capa`}
      width={width}
      height={height}
      className={cn('rounded-lg object-cover', className)}
    />
  )
}
```

Key details per UI-SPEC.md:
- Three sizes: sm (48x72 for home list), md (128x192 for mobile detail), lg (192x288 for desktop detail)
- Placeholder: zinc-800 background with BookOpen icon centered, `aria-label="Sem capa disponivel"`
- Image alt: `"{title} - capa"` per accessibility contract
- Uses `next/image` with pre-configured remotePatterns from Task 1
  </action>
  <verify>
    <automated>cd "/Users/mauro/projects/Dona Flora" && test -f src/components/star-rating.tsx && test -f src/components/status-badge.tsx && test -f src/components/book-cover.tsx && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/star-rating.tsx exists and contains `'use client'`
    - src/components/star-rating.tsx contains `min-w-[44px] min-h-[44px]` (touch target)
    - src/components/star-rating.tsx contains `aria-label` on each star button
    - src/components/star-rating.tsx contains `fill-yellow-400` for filled stars
    - src/components/status-badge.tsx exists and contains `STATUS_CONFIG` with all 5 statuses
    - src/components/status-badge.tsx contains `bg-blue-900/50` (lendo color)
    - src/components/status-badge.tsx contains `bg-green-900/50` (lido color)
    - src/components/book-cover.tsx exists and contains `import Image from 'next/image'`
    - src/components/book-cover.tsx contains `BookOpen` import from lucide-react (placeholder)
    - src/components/book-cover.tsx contains width and height values 48, 72, 128, 192, 288
    - `npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>StarRating renders 5 accessible clickable stars with yellow fill. StatusBadge renders colored label for each of the 5 statuses. BookCover renders next/image with placeholder fallback. All components follow zinc dark theme and UI-SPEC accessibility contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external image URL -> next/image | Book cover URLs from external APIs rendered via next/image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering | next.config.ts (image sources) | mitigate | `remotePatterns` restricts allowed image domains to `books.google.com` and `covers.openlibrary.org` only. No wildcard patterns. |
| T-02-09 | Information Disclosure | star-rating.tsx | accept | No sensitive data. Component is purely presentational with rating value 1-5. |
</threat_model>

<verification>
1. `npx tsc --noEmit` -- no TypeScript errors
2. `ls src/components/ui/ | wc -l` >= 9 shadcn components installed
3. `grep remotePatterns next.config.ts` -- image domains configured
</verification>

<success_criteria>
- shadcn/ui initialized and all 9 required components available in src/components/ui/
- next.config.ts allows images from Google Books and Open Library domains
- StarRating, StatusBadge, BookCover components created with correct theme and accessibility
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-core/02-03-SUMMARY.md`
</output>
